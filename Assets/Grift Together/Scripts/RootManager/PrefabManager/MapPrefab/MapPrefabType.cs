using UnityEngine;

namespace GriftTogether {
    public enum MapPrefabType {
        GameUIRoot,
        GameHeaderView,
        GameMenuView,
        
        PopUp,

        SessionUIRoot,
        SessionTurnView,

        RPCViem
    }
}
