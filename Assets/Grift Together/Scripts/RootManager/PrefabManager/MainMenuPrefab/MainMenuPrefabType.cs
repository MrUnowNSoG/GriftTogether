using UnityEngine;

namespace GriftTogether {
    public enum MainMenuPrefabType {
        RootView,
        HeaderView,
        MainView,
        PlayView,
        SkinView,
        SettingView,
        ExitView
    }
}
